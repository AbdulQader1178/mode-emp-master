import React from 'react'
import { default as RNDynamicVectorIcon } from 'react-native-dynamic-vector-icons'

const Icon = (props) => {
  return <RNDynamicVectorIcon type='AntDesign' {...props} />
}

export default Icon
