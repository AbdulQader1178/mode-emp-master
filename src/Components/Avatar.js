import React from 'react'
import { Avatar as RNUILibAvatar } from 'react-native-ui-lib'

const Avatar = (props) => {
  return (
    <RNUILibAvatar {...props} />
  )
}

export default Avatar
