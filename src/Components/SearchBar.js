import React from 'react'
import { Searchbar as RNPaperSearchBar } from 'react-native-paper'

// export const SEARCHBAR_HEIGHT = 60

const SearchBar = (props) => {
  return (
    <RNPaperSearchBar {...props} />
  )
}

export default SearchBar
