import { Colors, Typography, Spacings } from 'react-native-ui-lib'

Colors.loadColors(require('./Colors'))

Typography.loadTypographies(require('./Typography'))

Spacings.loadSpacings(require('./Spacings'))
